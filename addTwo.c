// C program to add two numbers
#include <stdio.h>

int main() {
    int a, b, sum = 0;

      // Read two numbers from the user
    printf("Enter two integers: ");
    scanf("%d %d", &a, &b);

    sum = a + b;

    printf("Sum: %d/n", sum);

    return 0;
}
